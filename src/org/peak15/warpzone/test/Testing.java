package org.peak15.warpzone.test;

//import java.util.HashSet;
//import java.util.Set;
//import java.io.File;
//import java.io.IOException;

//import org.peak15.warpzone.shared.*;

public class Testing {

	public static void main(String[] args) {
		/*Set<Player> players = new HashSet<Player>();
		
		Player ply = new Player("blarg");
		
		players.add(ply);
		
		System.out.println(ply.getPos().toString());
		
		for(Player player:players) {
			player.setPos(new Vector(1337, 1337));
		}
		
		System.out.println(ply.getPos().toString());*/
		
		/*int color = 0xff0000ff;
		
		byte[] colByte = NetworkStuff.toBytes(color);
		System.out.println(Integer.toHexString(color));
		System.out.println(Integer.toHexString(colByte[0]));
		if(colByte[0] == 0xffffffff) {
			System.out.println("yupp");
		}*/
		
		/*try {
			NetFile nf = new NetFile(new File("maps/test/test.png"), NetFile.FileType.MAP);
			File out = new File("maps/test/net.png");
			nf.write(out);
		} catch (IOException e) {
			e.printStackTrace();
		}*/
	}

}
